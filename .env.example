# Kalshi Quant Bot Configuration
# Copy this file to .env and fill in your values

# Environment: 'demo' (recommended) or 'prod'
KALSHI_ENV=demo

# API Key ID from Kalshi UI (Account settings → API keys)
KALSHI_API_KEY_ID=your-api-key-id-here

# Path to your private key file (.key/.pem) from Kalshi
KALSHI_PRIVATE_KEY_PATH=/path/to/private_key.pem

# Comma-separated list of market tickers to trade
# Example: FED-23DEC-T3.00,ANOTHER-TICKER
TICKERS=FED-23DEC-T3.00

# Optional: Fair probabilities as JSON (used by starter strategy)
# Example: {"FED-23DEC-T3.00": 0.42}
FAIR_PROBS_JSON={}

# Optional: Minimum edge threshold (default: 0.04)
EDGE_THRESHOLD=0.04

# Optional: Maximum number of orders (default: 10)
MAX_ORDER_COUNT=10

# Optional: Maximum position per ticker (default: 50)
MAX_POSITION_PER_TICKER=50

# Optional: Polling interval in seconds (default: 2.0)
POLL_SECONDS=2.0

# Optional: Demo API host (default: https://demo-api.kalshi.co/trade-api/v2)
# KALSHI_HOST_DEMO=https://demo-api.kalshi.co/trade-api/v2

# Optional: Production API host (default: https://api.elections.kalshi.com/trade-api/v2)
# KALSHI_HOST_PROD=https://api.elections.kalshi.com/trade-api/v2
